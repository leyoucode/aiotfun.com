---
import '../styles/global.css';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <script is:inline>
    (function(){
      var s = localStorage.getItem('theme');
      if (s === 'dark' || (!s && matchMedia('(prefers-color-scheme:dark)').matches))
        document.documentElement.classList.add('dark');
    })();
    </script>
    <meta name="color-scheme" content="light dark" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="/favicon.ico" sizes="16x16 32x32" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&family=Space+Grotesk:wght@500;700&display=swap"
      rel="stylesheet"
    />
    <title>404 — AIoTFun</title>
  </head>
  <body class="flex min-h-screen flex-col items-center justify-center bg-bg px-4 text-center">
    <a href="/" class="mb-8 flex items-center gap-2 font-body text-xl font-bold tracking-tight">
      <img src="/logo.png" alt="AIoTFun" class="h-8 w-8" />
      <span><span class="text-text">AIoT</span><span class="text-accent">Fun</span></span>
    </a>

    <p class="font-mono text-8xl font-bold text-accent">404</p>

    <p id="msg-main" class="mt-4 font-body text-lg text-text">
      This page wandered off into the IoT void.
    </p>
    <p id="msg-sub" class="mt-1 text-sm text-text-muted">
      Maybe the AI agents forgot to build it.
    </p>

    <a
      id="btn-home"
      href="/en/"
      class="mt-8 inline-block rounded-full bg-accent px-6 py-2.5 font-body text-sm font-semibold text-white transition-opacity hover:opacity-90"
    >
      Back to Home
    </a>

    <script is:inline>
      (function() {
        var zh = /^zh/i.test(navigator.language || '');
        if (zh) {
          document.getElementById('msg-main').textContent = '这个页面飘进了 IoT 的虚空。';
          document.getElementById('msg-sub').textContent = '也许 AI Agent 忘了生成它。';
          var btn = document.getElementById('btn-home');
          btn.textContent = '返回首页';
          btn.href = '/zh/';
          document.documentElement.lang = 'zh';
        }
      })();
    </script>
  </body>
</html>
